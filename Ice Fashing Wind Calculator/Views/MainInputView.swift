import SwiftUI

// MainInputView is no longer used — the single-page ContentView replaces it.
// File kept for Xcode project reference compatibility.

struct MainInputView: View {
    var body: some View {
        EmptyView()
    }
}
